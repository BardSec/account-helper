services:
  ad-password-reset:
    build: .
    # host networking lets the container resolve internal .local / AD hostnames
    # the same way the host machine does. ports: is not needed in this mode —
    # the app is accessible directly on the host's IP at $PORT.
    network_mode: host
    env_file:
      - .env
    restart: unless-stopped
